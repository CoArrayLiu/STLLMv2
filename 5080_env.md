RTX 5080 是 Blackwell，不能继续使用当前的 `torch 2.3.1 + cu118`。建议迁移到 `PyTorch 2.7.1 + CUDA 12.8`：这是首个正式支持 Blackwell 的 PyTorch 系列，而且相对接近项目当前版本，兼容风险比直接上最新 PyTorch 更低。[PyTorch 2.7 发布说明](https://pytorch.org/blog/pytorch-2-7/)、[官方历史版本安装命令](https://pytorch.org/get-started/previous-versions/)。

## 1. 检查显卡驱动

新机器先执行：

```bash
nvidia-smi
```

建议 Linux NVIDIA 驱动至少为 570 系列，CUDA 12.8 GA 要求驱动不低于 570.26。[NVIDIA CUDA 12.8 Release Notes](https://docs.nvidia.com/cuda/archive/12.8.0/cuda-toolkit-release-notes/index.html)

使用 PyTorch wheel 时不需要单独安装完整 CUDA Toolkit，显卡驱动正常即可。

## 2. 创建新 conda 环境

不要直接复制旧 conda 环境：

```bash
conda create -n st-llm-plus-5080 python=3.10 pip -y
conda activate st-llm-plus-5080

python -m pip install --upgrade pip setuptools wheel
```

## 3. 安装支持 RTX 5080 的 PyTorch

```bash
python -m pip install \
  torch==2.7.1 \
  torchvision==0.22.1 \
  torchaudio==2.7.1 \
  --index-url https://download.pytorch.org/whl/cu128
```

验证 Blackwell：

```bash
python - <<'PY'
import torch

print("torch:", torch.__version__)
print("CUDA build:", torch.version.cuda)
print("CUDA available:", torch.cuda.is_available())
print("GPU:", torch.cuda.get_device_name(0))
print("capability:", torch.cuda.get_device_capability(0))
print("architectures:", torch.cuda.get_arch_list())

x = torch.randn(1024, 1024, device="cuda")
y = x @ x
torch.cuda.synchronize()
print("CUDA test:", y.shape, y.device)
PY
```

正常应看到：

```text
CUDA available: True
GPU: NVIDIA GeForce RTX 5080
capability: (12, 0)
```

## 4. 安装项目基础依赖

```bash
python -m pip install \
  numpy==1.26.4 \
  scipy==1.15.3 \
  pandas==2.3.3 \
  Bottleneck==1.6.0 \
  transformers==4.36.2 \
  peft==0.11.1 \
  accelerate==0.31.0
```

保留原来的 Transformers/PEFT 版本，可以减少项目自定义 GPT-2 内部接口发生变化的风险。

## 5. 安装 PyG 和 CUDA 12.8 扩展

PyG 官方提供了 PyTorch 2.7/cu128 对应 wheel。[PyG 安装文档](https://pytorch-geometric.readthedocs.io/en/stable/notes/installation.html)、[PyG 2.7.1/cu128 wheel 索引](https://data.pyg.org/whl/torch-2.7.1%2Bcu128.html)。

```bash
python -m pip install torch-geometric

python -m pip install \
  pyg_lib \
  torch_scatter \
  torch_sparse \
  torch_cluster \
  torch_spline_conv \
  -f https://data.pyg.org/whl/torch-2.7.1+cu128.html
```

验证 PyG：

```bash
python - <<'PY'
import torch
import torch_geometric
import torch_scatter
import torch_sparse

from torch_geometric.nn import GCNConv

x = torch.randn(4, 8, device="cuda")
edge_index = torch.tensor(
    [[0, 1, 2, 3], [1, 2, 3, 0]],
    device="cuda",
)

out = GCNConv(8, 4).cuda()(x, edge_index)

print("PyG:", torch_geometric.__version__)
print("torch-scatter:", torch_scatter.__version__)
print("torch-sparse:", torch_sparse.__version__)
print("output:", out.shape, out.device)
PY
```

## 6. 检查环境并启动训练

```bash
python -m pip check
```

然后进入项目：

```bash
cd /path/to/ST-LLM-Plus

python -u train_plus.py \
  --device cuda:0 \
  --data taxi_pick \
  --batch_size 16
```

RTX 5080 通常只有 16 GB 显存，建议先从 `--batch_size 16` 开始；如果显存不足，降到 8。之前在 A100 80 GB 上并行跑四个任务的方式不适合直接搬到 RTX 5080，建议一次跑一个，或把每个任务 batch size 大幅降低。

当前 [requirement.txt](/root/exps/ST-LLM-Plus/requirement.txt) 是 A100/cu118 版本，不能原样用于 RTX 5080；其中 PyTorch 和 PyG CUDA 扩展需要按上面的 cu128 命令单独安装。